{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Api": {
    "BaseUrl": "http://localhost:5051",
    "TimeoutSeconds": 30
  },

  "Auth": {
    "LoginEndpoint": "/api/auth/login",
    "RefreshEndpoint": "/api/auth/refresh",
    "LocalFallback": {
      "Enabled": true,
      "Username": "admin",
      "Password": "Admin@123",
      "DisplayName": "مدير النظام",
      "Note": "Temporary bridge — replace with real JWT login once POST /api/auth/login exists in AuctionAPI."
    }
  }
}

